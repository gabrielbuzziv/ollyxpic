export default {
    'spots/GET_VOCATIONS' ({ vocations }) {
        return vocations
    },

    'spots/GET_SUPPLIES' ({ supplies }) {
        return supplies
    },

    'spots/GET_CREATURES' ({ creatures }) {
        return creatures
    },
}