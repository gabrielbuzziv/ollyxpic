export default {
    'waste/GET_POTIONS' ({ potions }) {
        return potions
    },

    'waste/GET_AMMUNITIONS' ({ ammunitions }) {
        return ammunitions
    },

    'waste/GET_RUNES' ({ runes }) {
        return runes
    },

    'waste/GET_AMULETS' ({ amulets }) {
        return amulets
    },

    'waste/GET_RINGS' ({ rings }) {
        return rings
    },
}